# Copia este fichero a terraform.tfvars y pon tu correo.
# terraform.tfvars esta en .gitignore: no se versiona.

budget_email = "tu-correo@ejemplo.com"

# Opcionales, con valores por defecto razonables:
# budget_amount = 10
# region        = "eu-west-1"
